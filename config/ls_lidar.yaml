/**:
    ros__parameters:
        feature_extract_enable: false
        point_filter_num: 4           # 增大可减少输入点数: 4→6~8 减少约33%~50%计算量
        max_iteration: 3              # IEKF迭代次数: 3→2 可大幅减少ICP时间
        filter_size_surf: 0.5         # 去畸变后体素降采样(m): 0.5→0.8~1.0 减少点数
        filter_size_map: 0.5          # 地图体素降采样(m): 0.5→0.8~1.0 减少KD树搜索
        cube_side_length: 1000.0      # 局部地图边长(m): 可考虑减小到500减少地图维护开销
        runtime_pos_log_enable: true    # 开启诊断日志，输出到 Log/ 目录
        map_file_path: "/home/lyx/Downloads/FAST_LIO2_ws/pcd/test.pcd"
        # === 快速播放防漂移参数 ===
        adaptive_downsample_en: true  # 启用自适应降采样: 处理慢时自动增大filter
        max_lidar_buffer_size: 3      # 雷达缓冲区上限: 超过则丢帧, 防止时间戳滞后

        common:
            lid_topic:  "/cx/lslidar_point_cloud"
            imu_topic:  "/imu/data"
            time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
            time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                        # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0

        preprocess:
            lidar_type: 2                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 
            scan_line: 32
            scan_rate: 10                # only need to be set for velodyne, unit: Hz,
            timestamp_unit: 2            # the unit of time/t field in the PointCloud2 rostopic: 0-second, 1-milisecond, 2-microsecond, 3-nanosecond.
            blind: 0.5

        mapping:
            acc_cov: 0.5               # 加速度噪声协方差: 增大会降低对IMU的信任,更多依赖雷达
            gyr_cov: 0.5               # 陀螺仪噪声协方差: 同上, 快速播放时IMU积分误差大, 应增大
            b_acc_cov: 0.0005          # 加速度bias协方差
            b_gyr_cov: 0.0005          # 陀螺仪bias协方差
            fov_degree:    360.0
            det_range:     100.0
            extrinsic_est_en:  false      # true: enable the online estimation of IMU-LiDAR extrinsic,
            extrinsic_T: [ 0.073335, -0.171632, 0.672794]
            extrinsic_R: [ 0.992826,  0.118127, -0.018489,
                          -0.117737,  0.992824,  0.020945,
                           0.020831, -0.018618,  0.999610]

        publish:
            path_en:  true
            scan_publish_en:  true       # false: close all the point cloud output
            dense_publish_en: true       # false: low down the points number in a global-frame point clouds scan.
            scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame
            effect_map_en: true
            map_en: true

        pcd_save:
            pcd_save_en: false
            interval: -1                 # how many LiDAR frames saved in each pcd file; 
                                        # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
